{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "turbo run build --filter=@github-users/docs && changeset publish "
  },
  "devDependencies": {
    "@changesets/cli": "^2.26.0",
    "@types/node": "^24.9.1",
    "@types/react": "^19.2.2",
    "turbo": "^2.5.8"
  },
  "packageManager": "npm@10.9.3"
}
